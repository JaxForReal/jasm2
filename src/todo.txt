make easier to generate code (compiler into jasm2)

how to retrieve addresses at non-32bit values

consider using i32 instead of u32 for memory segments...?
or smaller segmentation?

TESTING!

fix enumerates not working...

remove need for semicolons at end of line

<start>;
data 96 98 97 100 99 -> 10;
valueof 10 -> 0;
valueof 4 -> 1;
call print_all_ascii;
ret;


//@0 = poniter to data
//@1 = # of cells to print

<print_all_ascii>;
//copy to 5 and 6 so we can use syscall without destroying
valueof @0 -> 5;
valueof @1 -> 6;

valueof @@5 -> 0;
syscall print_ascii;
//incement pointer
add @5 1 -> 5;
//subtract from counter
sub @6 1 -> 6;
jumpnotzero print_all_ascii;
ret;